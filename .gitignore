dist-newstyle/
cabal.project.local
*.tix
.hpc/
h-gpgme-*.tar.gz

.idea/
h-gpgme.iml

# gpg writes these into the test homedirs while the tests run. Everything else
# under test/alice, test/bob and test/carol is a fixture and belongs in git.
test/*/random_seed
test/*/tofu.db
test/*/*~
test/*/.#*
test/*/S.gpg-agent*
test/*/openpgp-revocs.d/
